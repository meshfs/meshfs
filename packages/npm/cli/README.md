# @meshfs/cli

npm distribution for MeshFS CLI.

## Install

```bash
npm install -g @meshfs/cli
```

## Usage

```bash
meshfs --help
```

The package resolves a platform-specific binary package and executes the native `meshfs` executable.
